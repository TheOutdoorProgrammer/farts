apiVersion: v1
kind: Service
metadata:
  name: {{ include "farts.fullname" . }}
  labels:
    {{- include "farts.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "farts.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
